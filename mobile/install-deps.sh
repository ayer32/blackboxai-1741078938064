#!/bin/bash

# Install required type definitions
npm install --save-dev @types/node
npm install --save-dev @types/lottie-react-native

# Install expo type definitions
npm install --save-dev @types/expo
npm install --save-dev @types/expo-camera
npm install --save-dev @types/expo-blur

# Make sure all dependencies are installed
npm install
